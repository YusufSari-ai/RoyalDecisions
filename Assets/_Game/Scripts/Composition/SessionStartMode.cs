namespace RoyalDecisions.Composition
{
    /// <summary>
    /// How the game scene should start, chosen before it loads.
    /// </summary>
    public enum SessionStartMode
    {
        NewGame = 0,
        Continue = 1
    }
}
